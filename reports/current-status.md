# Moved

The current gameweek's picks now live on the [front page](../README.md)
directly, with a running total across all weeks at the top. Past
gameweeks are archived individually — see
[Matchweek 4](matchweek-4.md).

This file is kept as a pointer rather than deleted, per this project's
own rule against silently removing history.
